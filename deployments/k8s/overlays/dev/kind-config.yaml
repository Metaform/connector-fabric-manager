# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30081    # PManager
        hostPort: 8181
        protocol: TCP
      - containerPort: 30080    # TManager
        hostPort: 8282
        protocol: TCP

